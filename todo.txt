- write a cron job to update the win/loss outcome of all survivor pool / pickems each week tues@23:50:00 (10min before midnight) by calling cron-jobs\run-update.js

- if scores are reprocessed, do the autopicks that are random get recalculated? 
- limitation: if scores are reprocessed for a past week, users who missed picks could have autos assigned (they werent active user that week)

